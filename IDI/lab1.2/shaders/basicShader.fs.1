#version 330 core

out vec4 FragColor;

void main() {
    FragColor = vec4(
        gl_FragCoord.x < 354 ? 1 : 0,
        gl_FragCoord.y < 354 ? 1 : 0,
        gl_FragCoord.x > 354 ? 1 : 0,
        1
    );
}

