#DISCRETO TODO
# SI UNA GRÁFICA TIENE FORMA DE CAMPANA DE GAUSS CON MEDIA PARECIDA A VARIANZA PUEDE SER
#     BINOMIAL (p PEQUEÑA)
#     POISSON

# SI UNA GRÁFICA TIENE FORMA DE CAMPANA DE GAUSS CON MEDIA DISTINTA A VARIANZA PUEDE SER
#     BINOMIAL

# SI UNA GRÁFICA ES DECRECIENTE
#     GEOMETRICA



#MULTIPLES OPCIONES
# SI SON DATOS DISCRETOS (ENTEROS), SE PARECE A UNA BINOMIAL O POISSON 
# SI NO SON DATOS DISCRETOS (NO ENTEROS), SE PARECE A UNA NORMAL


# SI SON DATOS DISCRETOS (ENTEROS), SE PARECE A UNA UNIFORME
# SI NO SON DATOS DISCRETOS (NO ENTEROS), SE PARECE A UNA UNIFORME CONTINUA

# SI SON DATOS DISCRETOS (ENTEROS), SE PARECE A UNA GEOMÉTRICA
# SI NO SON DATOS DISCRETOS (NO ENTEROS), SE PARECE A UNA EXPONENCIAL

# SI MEDIA=VARIANZA POISSON O BINOMIAL SINO BINOMIAL
mean(dat1) #12.49
var(dat1) #6.574474

#CASO POISSON O BINOMIAL
p<-1-var(dat3)/mean(dat3)
p
# si p pequeña binomial sino poisson

#CASO POISSON
lambda<-mean()

#CASO BINOMIAL
p1<-1-var(dat1)/mean(dat1)
n1<-mean(dat1)/p1
c(n1,p1)

#UNIFORME CONTINUA
a<-min(dat2)
b<-max(dat2)
p2<-1/(b-a)

#EXPONENCIAL
lambda<-mean(dat3)

#NORMAL
mu<-mean(dat4)
sigma<-sd(dat4)
c(mu,sigma)

#GEOMÉTRICA
p<-1/(1+mean(dat4))
p


#GRAFICO QQ
Q<-seq(0,1,0.1)
Qdat1<-quantile(dat1,Q)
Qdat1teo<-qbinom(Q,27,p1) #CAMBIA SEGÚN DISTRIBUCIÓN
plot(Qdat1,Qdat1teo)
abline(0,1,col="red")

# FUNCIONES PARA LUEGO VER SU DISTRIBUCION
f3=function(){return(-log(1-runif(1,0,1)))}
simuldatos3<-c()

simuldatos3<- -log(1-runif(100000,0,1)) #CAMBIA FUNCION SEGUN EJERCICIO
hist(simuldatos3)
